{
  "name": "talons-agent-api",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker API for GitHub Pages AI assistant",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test src/index.test.js"
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.12.0"
  }
}
